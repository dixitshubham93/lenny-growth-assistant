# ── Lenny Growth Assistant — Backend Dependencies ─────────────────────────────
# Phase 2: FastAPI + LLM provider foundation
# Pin major versions; allow patch updates.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.1
uvicorn[standard]>=0.30.1

# Pydantic v2 (FastAPI 0.111 requires pydantic v2)
pydantic>=2.9.2
pydantic-settings>=2.4.0

# HTTP client — used by OllamaProvider to call the local Ollama REST API
httpx==0.28.1

# Groq cloud LLM SDK (>=0.11.0 required for httpx 0.28 compatibility)
groq==1.6.0

# Environment variable loading
python-dotenv==1.0.1

# ── Phase 3: Database ─────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.31   # async ORM
asyncpg==0.29.0               # PostgreSQL async driver (production)
alembic==1.13.2               # schema migrations
aiosqlite==0.20.0             # SQLite async driver (unit test fixtures only)

# ── Phase 4: Ingestion pipeline ───────────────────────────────────────────────
PyYAML>=6.0                   # YAML frontmatter parsing in ingestion/parse.py
# httpx already listed above — also used in ingestion/fetch.py

# ── Phase 5: pgvector / RAG ────────────────────────────────────────────────────
pgvector>=0.3.0               # VECTOR(768) column type + cosine distance for SQLAlchemy

# ── Phase 6: Agent Layer ───────────────────────────────────────────────
claude-agent-sdk>=0.2.144    # Anthropic Claude Agent SDK (ClaudeSDKClient, @tool)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx  # already listed above; pytest uses httpx via starlette TestClient
