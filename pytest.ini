[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# backend/ provides `from app.*` (Phase 2/3 tests)
# .       provides `from ingestion.*` (Phase 4 tests)
pythonpath = backend .
